#include <stdio.h>

int main(void) {
    /*
     * TODO 1: Declare variables for two numbers and one operator.
     * TODO 2: Read one complete line in the form: number operator number.
     * TODO 3: Reject incomplete, negative, unknown, or trailing input.
     * TODO 4: Use if / else to calculate and print the required result.
     * TODO 5: Print exactly one result line, or one Error line.
     */
    return 0;
}
