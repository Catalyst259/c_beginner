#include "lab01.h"

int total_power(const int items[], int count) {
    /* TODO 1: Visit every item once and return the accumulated power. */
    (void)items;
    (void)count;
    return 0;
}

int strongest_item_index(const int items[], int count) {
    /*
     * TODO 2: Track the index of the strongest item. When powers tie, keep
     * the first index.
     */
    (void)items;
    (void)count;
    return 0;
}

int collect_qualified(const int items[], int count,
                      int minimum, int qualified[]) {
    /*
     * TODO 3: Copy every power >= minimum to qualified without changing
     * its order, then return the number copied.
     */
    (void)items;
    (void)count;
    (void)minimum;
    (void)qualified;
    return 0;
}

void sort_descending(int items[], int count) {
    /* TODO 4: Use nested loops and swaps to sort items from high to low. */
    (void)items;
    (void)count;
}

int best_pair_power(const int items[], int count, int limit) {
    /*
     * TODO 5: Check pairs at different indices and return the largest sum
     * that does not exceed limit, or -1 when no valid pair exists.
     */
    (void)items;
    (void)count;
    (void)limit;
    return -1;
}
